<form method="post" action="{{ path('app_movie_quote_delete', {'id': movie_quote.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ movie_quote.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
